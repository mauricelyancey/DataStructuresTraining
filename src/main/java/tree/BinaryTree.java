package tree;

import java.util.List;

public class BinaryTree {

    public void buildTree(List<Node> nodes, Tree tree){
        if(tree.getRoot()==null) tree.setRoot(nodes.get(0));
        else if ()
    }
}
